{
  "name": "chat_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build tsconfig.json",
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "start": "nodemon --config nodemon.json src/server.ts",
    "start:debug": "nodemon --config nodemon.json --inspect-brk src/server.ts",
    "builddev": "docker build -t myapp -f Dockerfile-dev .",
    "buildprod": "./build_production_image.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/express-session": "^1.17.3",
    "@types/node": "^14.14.35",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "tslint": "^6.1.3",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-session": "^1.17.1",
    "openid-client": "^4.6.0"
  }
}
